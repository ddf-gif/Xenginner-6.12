/**
 * WebSocket client (stub — implemented in PR pr/e2e-integration).
 * Manages browser↔backend WebSocket connection with auto-reconnect.
 */
const WsClient = (() => {
    console.log('WsClient stub loaded');
    return { /* to be implemented in pr/e2e-integration */ };
})();
