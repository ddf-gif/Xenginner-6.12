/**
 * Video frame capture pipeline (stub — implemented in PR pr/video-capture).
 * Captures camera frames at 1fps, resizes to 480p, encodes as JPEG base64.
 */
const VideoCapture = (() => {
    console.log('VideoCapture stub loaded');
    return { /* to be implemented in pr/video-capture */ };
})();
