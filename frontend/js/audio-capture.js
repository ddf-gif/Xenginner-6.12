/**
 * Audio capture pipeline (stub — implemented in PR pr/audio-capture).
 * Captures microphone audio, converts to PCM16 16kHz, outputs 100ms base64 chunks.
 */
const AudioCapture = (() => {
    console.log('AudioCapture stub loaded');
    return { /* to be implemented in pr/audio-capture */ };
})();
